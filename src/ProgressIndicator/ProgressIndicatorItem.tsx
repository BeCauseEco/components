import { Color } from "@new/Color"

export type ProgressIndicatorItemProps = {
  width: `${number}%`
  color: Color
}

export const ProgressIndicatorItem = ({
  // eslint-disable-next-line
  width,

  // eslint-disable-next-line
  color,
}: ProgressIndicatorItemProps) => null
