import { TColor } from "@new/Color"

export type TChartItem = {
  dataKey: string
  color: TColor
}

export const ChartItem = ({
  // eslint-disable-next-line
  dataKey,
  // eslint-disable-next-line
  color
}: TChartItem) => null
